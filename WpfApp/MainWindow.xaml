<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp"
        Title="Скидки пользователей" Height="720" Width="1280" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="ButtonStyle1" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15 5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                            CornerRadius="8"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#45a049"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3d8b40"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                           
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="20">
            <Border BorderBrush="Black" BorderThickness="1" Padding="10">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding Companies}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="10">
                                    <Grid Background="#FFF4E8D3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Background="#FFF4E8D3">
                                            <TextBlock Text="Название компании" FontWeight="Bold" FontSize="16"/>
                                            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left" FontSize="14" SelectionBrush="#FF67BA80" />
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Width="200" HorizontalAlignment="Left"/>
                                            <TextBlock Text="Уровень скидки" FontSize="16" Width="200" HorizontalAlignment="Left" />
                                            <ComboBox ItemsSource="{Binding Source={x:Static local:DiscountTypeHelper.Values}}"
                                                      SelectedItem="{Binding DiscountLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Width="200" HorizontalAlignment="Left" />
                                            <TextBlock Text="Количество товаров" FontSize="16" Width="200" HorizontalAlignment="Left" />
                                            <TextBox Text="{Binding ProductCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Width="200" HorizontalAlignment="Left" SelectionBrush="#FF67BA80" />
                                            
                                        </StackPanel>

                                        <TextBlock Grid.Column="1" Text="{Binding TotalDiscount, StringFormat='{}{0}%'}"
                                                   FontWeight="Bold" VerticalAlignment="Center" Margin="10"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Button Style="{DynamicResource ButtonStyle1}" Content="Рассчитать скидку" Margin="10" Padding="10"
                    HorizontalAlignment="Center" Click="CalculateDiscounts" Background="#FF67BA80" FontSize="16" Foreground="Black" BorderBrush="#FF37B150" Height="45" Width="200" Panel.ZIndex="3" />
        </StackPanel>
    </Grid>
</Window>